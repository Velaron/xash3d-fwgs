#! /usr/bin/env python
# encoding: utf-8

def options(opt):
	pass

def configure(conf):
	if not conf.path.find_dir('openxr') or not conf.path.find_dir('openxr/src'):
		conf.fatal('Can\'t find openxr submodule. Run `git submodule update --init --recursive`.')
		return